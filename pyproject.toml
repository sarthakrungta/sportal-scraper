[project]
name = "sportal-cron"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = ["psycopg2-binary", "selenium", "beautifulsoup4"]
